[defaults]
inventory = inventory.yml
stdout_callback = yaml
interpreter_python = auto_silent
retry_files_enabled = False
# Silence deprecation warnings leaking from the containers.podman connection
# plugin (upstream, not actionable in this playbook).
deprecation_warnings = False
